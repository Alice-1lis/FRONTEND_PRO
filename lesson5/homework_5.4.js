/*const input = +prompt("Введіть число: ");

function isPrime(num) {
    if (num <= 1) {
        return false;
    }
    for (let i = 2; i <= Math.sqrt(num); i++) {
        if (num % i === 0) {
            return false;
        }
    }
    return true;
}
if (isNaN(input)) {
    console.log("Ви ввели не число");
} else {
    if (isPrime(input)) {
        console.log(`${input} є простим числом.`);
    } else {
        console.log(`${input} не є простим числом.`);
    }
}*/