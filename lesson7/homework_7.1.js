/*let str = prompt("Введите строку: ");
let chars = prompt("Введите символы для удаления: ");

alert(clearchars(str, chars));

function clearchars(str, chars) {
    let result = '';
    for (let i = 0; i < str.length; i++) {
        if (!chars.includes(str[i])) {
            result += str[i];
        }
}   
 return result;
}*/