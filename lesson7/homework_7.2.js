/*const array = [1, 'hello world', 3, true, 5, 30];

function getNumberArray(arr) {

   let sum = 0;
   let count = 0;

    for (let i = 0; i < arr.length; i++) {
        if (typeof arr[i] === 'number') {
            sum += arr[i];
            count++;
        }
    }
    const result = count > 0 ? sum / count : 0;
    console.log("середнє арифметичне:", result);
    
}
getNumberArray(array);*/